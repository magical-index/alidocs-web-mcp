# 安全策略

## 支持版本

本项目处于早期阶段（0.x），安全修复只在最新版本上进行。

## 报告漏洞

**请勿在公开 issue / PR / 讨论区披露安全问题。**

请通过私下渠道报告（GitHub Security Advisory：仓库 Security → Report a vulnerability）。报告请尽量包含：

- 影响的版本与运行环境（OS / Node 版本 / 使用的 MCP host）
- 攻击者模型：属于哪个方向（见 [docs/security.md §1](./docs/security.md)）——恶意网页、本地恶意进程、端口冒充者、还是分发/提示投毒
- 复现步骤与最小复现物
- 你认为受影响的资产（文档内容读取？写入？凭证泄露？）

我们会在确认后与你商定披露时间线。

## 本项目的安全模型

在报告前，建议先读 [docs/security.md](./docs/security.md)，其中定义了：

- 四个攻击方向及各自的正交防御
- S1–S13 措施清单
- **已知残余风险**（§6）：同源 XSS、已具备用户权限的本地恶意进程、浏览器本地网络策略收紧、固定端口冲突

若你的发现属于 §6 已声明的残余风险，我们仍欢迎报告（可能有更好的缓解方案），但它不会被视为新漏洞。

## 特别关注的高危改动

以下任一情况被视为严重问题，请优先报告：

- 能让**非白名单 Origin** 的页面建立会话
- 能在**不知道配对码**的情况下完成握手
- 配对码明文出现在网络传输、URL、日志或公开产物中
- `/health` 向白名单外来源泄露 `connected` / `allowWrite` 等状态
- 存在任何「桥返回代码 → 调用方执行」的路径（违反 A2 红线）
